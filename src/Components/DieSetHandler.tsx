import React from 'react';
import { Button } from '../Style/inputStyle';

export const DieSetHandler = () => {
    return (
        <div>
            <Button fluid>Change Die Set</Button>
        </div>
    )
}